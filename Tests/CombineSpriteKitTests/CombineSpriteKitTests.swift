import XCTest
@testable import CombineSpriteKit

final class CombineSpriteKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CombineSpriteKit().text, "Hello, World!")
    }
}
